extends layout

block content
  h1.title #{title} 

    
  div.card
    div.row
        div.container
            h2 Name : #{item.name} 
            p  Id : #{item._id} 
            h4 £ #{item.sellingprice} 
           
  div.listitems
    h4 Other item details
    ul  
      li
        p Description : #{item.description}
      li 
        p Type : #{item.type}
    



  h4 Do you really want to delete #{item.name}  ?

    form(method='POST' action='')
        div.form-group
        input#itemid.form-control(type='hidden',name='itemid', required='true', value=item._id )

        button.btn.btn-primary(type='submit') Delete
